amqpinput:
  serveruri: "amqp://guest:guest@localhost:5672/"

  channels:
    - name: "test"
      required: true
      durable: false
      autodelete: false

      # These are not yet implemented
      maxbatchsize: 100
      minintervalms: 100
      maxintervalms: 1000
      maxmessagespersec: 10
